{
  "name": "pinterest-url-audit-action",
  "version": "1.0.0",
  "description": "Audit and normalize Pinterest URLs in GitHub repositories.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "ncc build src/index.js -o dist --minify",
    "test": "node --test test/*.test.js",
    "package": "npm test && npm run build"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/glob": "^0.7.0",
    "pinterest-url-normalizer": "^0.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
